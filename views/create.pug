extends layout

block title
  | Add 2FA Provider

block content
  h1 Add new 2FA provider
  if message
    p #{message}
    
  h4= "Important notes"
  ol
    li= "These codes are technically visible to me, the site owner. If there's enough demand I might create a system to encrypt them make them unreadable to me."
    li= "Store this code securely somewhere! Write it in a book on your bookshelf."
  br
  form(action="/create" method="POST")
    table
      tr
        td
          label(for="providerName") Provider Name: 
        td
          input#providerName(type="text" name="name" required maxlength="50")
      tr
        td
          label(for="providerCode") Provider Code: 
        td
          input#providerCode(type="text" name="code" required maxlength="50")
    br
    button(type="submit") Add Provider
    br