{
  "name": "totp",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "startDev": "node server.js",
    "startProd": "pm2 start /usr/lib/totp/server.js",
    "stopProd": "pm2 delete server",
    "reloadProd": "git pull && sudo ./setup && pm2 reload all"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "totp-generator": "^1.0.0"
  }
}
