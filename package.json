{
  "name": "topt",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "reload": "git pull && sudo ./setup && pm2 reload"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "totp-generator": "^1.0.0"
  }
}
